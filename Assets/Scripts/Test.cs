using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;
using UnityEditor;

public class Test : MonoBehaviour
{

    //[SerializeField]
    //TMPro.TMP_SpriteAsset asset;
    //// Start is called before the first frame update
    //[ContextMenu("fix")]
    //void FakeStart()
    //{
    //    uint x = 0x20;
    //    foreach (var item in asset.spriteCharacterTable)
    //    {
    //        item.unicode =x;
    //        x++;
    //        EditorUtility.SetDirty(asset);
    //        asset.SetDirty();
    //    }
    //    asset.SetDirty();
    //    EditorUtility.SetDirty(asset);
    //    asset.SetDirty();
    //    EditorUtility.SetDirty(asset);
    //}

  
}
